/// Lesson 01 — Hello, World!
///
/// Every Dart program starts at the top-level `main()` function.
///
/// Run it from the repo root with:
///   dart run lessons/01_hello_world/main.dart
void main() {
  // `print` writes a line to the console — your window into what the code does.
  print('Hello, Dart!');
}
